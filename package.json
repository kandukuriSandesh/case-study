{
  "name": "case-study",
  "private": true,
  "scripts": {
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "start:all": "concurrently \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "setup-and-start": "npm run setup && npm run start:all"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
